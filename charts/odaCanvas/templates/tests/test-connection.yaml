apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "odaCanvas.fullname" . }}-test-connection"
  labels:
    {{- include "odaCanvas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "odaCanvas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
