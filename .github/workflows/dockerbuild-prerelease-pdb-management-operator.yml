name: Build PDB-Management-Operator PRERELEASE

on:
  pull_request:
    branches:
      - main
    paths:
      - 'source/operators/pdb-management/**'
      - '.github/workflows/dockerbuild-prerelease-pdb-management-operator.yml'
      - 'charts/canvas-oda/charts/pdb-management-operator/**'

jobs:
  build-pdb-management-operator:
    if: ${{ github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: tmforumodacanvas
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: tmforumodacanvas/pdb-management-operator
          tags: |
            type=raw,value=${{ github.event.pull_request.number }}-{{sha}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: source/operators/pdb-management
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max