apiVersion: oda.tmforum.org/v1alpha1
kind: privatevault
metadata:
  name: privatevault-vault-one
  namespace: privatevault-system
spec:
  name: vault-one
  type: sideCar
  sideCar:
    port: 5000
    token: 
      negotiate
  podSelector:
    namespace: demo-comp
    name: demo-comp-one-*
    serviceAccount: default

