apiVersion: v1
kind: Service
metadata:
  name: {{ include "canvas-portal.backend.fullname" . }}
  labels:
    {{- include "canvas-portal.backend.labels" . | nindent 4 }}
  {{- with .Values.backend.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    {{- include "canvas-portal.backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
  {{- with .Values.backend.service.loadBalancerIP }}
  loadBalancerIP: {{ . | quote }}
  {{- end }}
